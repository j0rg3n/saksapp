@model IReadOnlyList<SaksAppWeb.Models.Meeting>

@{
    ViewData["Title"] = "Møter";
}

<div class="d-flex justify-content-between align-items-center">
    <h1>Møter</h1>
    <a class="btn btn-primary" asp-action="Create">Nytt møte</a>
</div>

<table class="table table-sm table-striped">
    <thead>
    <tr>
        <th>Dato</th>
        <th>Id</th>
        <th>År/#</th>
        <th>Sted</th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var m in Model)
    {
        <tr>
            <td><a asp-action="Details" asp-route-id="@m.Id">@m.MeetingDate</a></td>
            <td>@m.Id</td>
            <td><a asp-action="Details" asp-route-id="@m.Id">@m.Year/@m.YearSequenceNumber</a></td>
            <td>@m.Location</td>
            <td class="text-end">
                <a class="btn btn-sm btn-outline-secondary" asp-action="Details" asp-route-id="@m.Id">Åpne</a>
            </td>
        </tr>
    }
    </tbody>
</table>
