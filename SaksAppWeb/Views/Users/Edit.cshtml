@model SaksAppWeb.Models.ApplicationUser

@{
    ViewData["Title"] = "Rediger brukernavn";
}

<h1>Rediger navn for @Model.Email</h1>

<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            @Html.AntiForgeryToken()
            <input type="hidden" name="id" value="@Model.Id" />
            <div class="mb-3">
                <label class="form-label">Fullt navn</label>
                <input name="fullName" class="form-control" value="@Model.FullName" />
            </div>
            <button type="submit" class="btn btn-primary">Lagre</button>
            <a asp-action="Index" class="btn btn-outline-secondary">Avbryt</a>
        </form>
    </div>
</div>
